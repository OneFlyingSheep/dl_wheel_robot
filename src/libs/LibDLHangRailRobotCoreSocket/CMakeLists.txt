find_package( Qt5Widgets REQUIRED )

IF (NOT SOURCES)
    FILE(GLOB_RECURSE SOURCES "*.cpp")
ENDIF (NOT SOURCES)

SET(EXTRA_LIBS
	LibCommonSocket
)

ADD_LIBRARY(LibDLHangRailRobotCoreSocket ${SOURCES}) 
TARGET_LINK_LIBRARIES(LibDLHangRailRobotCoreSocket ${Qt5Widgets_LIBRARIES} ${EXTRA_LIBS})