find_package( Qt5Widgets REQUIRED)

IF (NOT SOURCES)
    FILE(GLOB_RECURSE SOURCES "*")
ENDIF (NOT SOURCES)


ADD_LIBRARY(LibFftreal ${SOURCES})
TARGET_LINK_LIBRARIES(LibFftreal ${EXTRA_LIBS} ${Qt5Network_LIBRARIES} ${Qt5Widgets_LIBRARIES} ${Qt5Multimedia_LIBRARIES})
