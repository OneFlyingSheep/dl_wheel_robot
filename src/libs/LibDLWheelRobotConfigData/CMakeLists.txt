IF (NOT SOURCES)
    FILE(GLOB_RECURSE SOURCES "*.cpp")
ENDIF (NOT SOURCES)

SET(EXTRA_LIBS
     ##ADD LIB FILE HERE
	 LibDLWheelRobotDBOperation 
)

ADD_LIBRARY(LibDLWheelRobotConfigData ${SOURCES})
TARGET_LINK_LIBRARIES(LibDLWheelRobotConfigData ${EXTRA_LIBS})