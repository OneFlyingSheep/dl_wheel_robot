IF (NOT SOURCES)
    FILE(GLOB_RECURSE SOURCES "*.cpp")
ENDIF (NOT SOURCES)

SET(EXTRA_LIBS
     ##ADD LIB FILE HERE
)

ADD_LIBRARY(LibDLHangRailRobotConfigData ${SOURCES})
TARGET_LINK_LIBRARIES(LibDLHangRailRobotConfigData ${EXTRA_LIBS})