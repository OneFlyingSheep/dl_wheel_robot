find_package( Qt5Widgets REQUIRED )
IF (NOT SOURCES)
    FILE(GLOB_RECURSE SOURCES "*.h" "*.cpp")
ENDIF (NOT SOURCES)

SET(EXTRA_LIBS
     ##ADD LIB FILE HERE
	 LibQJson
)

ADD_LIBRARY(LibMapReader ${SOURCES}) 
TARGET_LINK_LIBRARIES(LibMapReader ${Qt5Widgets_LIBRARIES} ${EXTRA_LIBS})